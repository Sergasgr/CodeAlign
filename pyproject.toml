[project]
name = "codealign"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "bitsandbytes>=0.50.0",
    "datasets>=5.0.0",
    "peft>=0.19.1",
    "ruff>=0.16.0",
    "torch>=2.13.0",
    "transformers>=5.14.1",
    "trl>=1.9.1",
    "wandb>=0.28.1",
]

[dependency-groups]
dev = [
    "jupyter>=1.1.1",
    "pytest>=9.1.1",
]
